const UI=window.UI={},sc=document.querySelector('#screen'),ov=document.querySelector('#overlay');UI.render=h=>{sc.innerHTML=h;C.top()};UI.toast=x=>{let e=document.createElement('div');e.className='toast';e.textContent=x;document.body.appendChild(e);setTimeout(()=>e.remove(),1600)};UI.overlay=h=>{ov.innerHTML=`<div class="sheet"><button class="close" onclick="UI.close()">×</button>${h}</div>`;ov.classList.remove('hidden')};UI.close=()=>ov.classList.add('hidden');UI.navMap=()=>S.modern?UI.confirmReturn():UI.map();UI.map=()=>{let x=Object.entries(D.locations).map(([id,l])=>{let c=C.travel(id);return `<button class="btn" onclick="C.go('${id}')"><b>${l.name}</b><br><span class="muted">${id===S.place?'你在这里，不消耗体力':`约 ${c.m} 分钟 · 体力消耗约 ${c.e}`}</span></button>`}).join('');UI.render(`<div class="hero"><h2>青石镇 · ${C.clock()}</h2><p>当前位置：${D.locations[S.place].name}</p></div><div class="card"><b>体力 ${S.energy}%</b><div class="bar energy"><i style="width:${S.energy}%"></i></div></div><div class="grid">${x}</div><button class="btn primary" onclick="UI.restChoice()">回土地庙休息</button>`)};UI.energyWarning=(to,e)=>UI.overlay(`<h3>体力不足</h3><p>前往「${D.locations[to].name}」预计需要约 ${e} 点体力。</p><button class="btn" onclick="UI.tryEat('${to}')">吃点东西</button><button class="btn" onclick="UI.close();UI.restChoice()">回土地庙休息</button><button class="btn" onclick="UI.close()">取消</button>`);UI.tryEat=to=>{if(C.eat()){UI.close();C.go(to)}};UI.collapseEvent=ev=>UI.overlay(`<h3>你实在走不动了</h3><p><b>${ev[0]}</b></p><p>${ev[1]}</p><p>回去方式：${ev[2]}<br>花费：${ev[3]}文</p><button class="btn primary" onclick="UI.close();UI.restChoice()">到了土地庙……</button>`);UI.restChoice=()=>{S.place=S.home;UI.overlay(`<h3>土地庙 · 休息</h3><button class="btn" onclick="UI.close();C.rest(2)">休息2小时</button><button class="btn" onclick="UI.close();C.rest(4)">休息4小时</button><button class="btn primary" onclick="UI.close();C.sleep()">睡到第二天</button>`)};UI.relations=()=>UI.overlay(`<h3>人物</h3>${Object.entries(D.npcs).map(([k,n])=>`<div class="card"><b>${n[0]}</b> ♥${S.aff[k]??n[2]}<br><span class="muted">${n[1]}</span></div>`).join('')}`);UI.skills=()=>UI.overlay(`<h3>技能</h3><div class="card">🎣 捕鱼 ${S.skills.fishing}</div><div class="card">🌱 种植 ${S.skills.farming}</div><div class="card">🪚 木工 ${S.skills.wood}</div><div class="card">⚕ 医药 ${S.skills.medicine}</div><div class="card">💰 经商 ${S.skills.trade}</div><div class="card">🏪 现代零售 ${S.skills.retail}</div><div class="card">💬 人情 ${S.skills.social}</div>`);UI.rumors=()=>UI.overlay(`<h3>见闻</h3><p class="muted">只记录和任务、供货、重要发现相关的内容。</p>${S.rumors.map(x=>`<div class="card">${x}</div>`).join('')||'<p>暂时没有重要见闻。</p>'}`);UI.bags=()=>{let modern=S.modern;UI.overlay(`<h3>背囊</h3>${UI.inv('穿越背囊',S.bags.travel,modern)}${UI.inv('古代背囊',S.bags.ancient,false)}${UI.inv('土地庙储物',S.bags.home,false)}${modern?'<p class="muted">人在现代时，可以在这里把穿越背囊里的商品退回货架，腾出格子后再去便利店买别的。</p>':''}`)};UI.inv=(n,i,refund)=>`<div class="card"><b>${n}</b>${Object.entries(i.items).map(([k,v])=>`<div class="slot">${D.items[k].name} ×${v}${refund?` <button onclick="UI.refundFromBag('${k}')">退回1个</button>`:''}${i===S.bags.ancient&&D.items[k].food?` <button onclick="C.eat();UI.bags()">吃</button>`:''}</div>`).join('')||'<p class="muted">空</p>'}</div>`;UI.portalShortcut=()=>{if(S.modern)return UI.confirmReturn();if(S.place!=='hill')return UI.overlay(`<h3>要去后山穿越吗？</h3><p>快捷键会先前往后山，再进入神秘洞。</p><button class="btn primary" onclick="UI.close();C.go('hill')">前往后山</button><button class="btn" onclick="UI.close()">取消</button>`);UI.portal()};