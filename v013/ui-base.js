const UI=window.UI={},sc=document.querySelector('#screen'),ov=document.querySelector('#overlay');UI.render=h=>{sc.innerHTML=h;C.top()};UI.toast=x=>{let e=document.createElement('div');e.className='toast';e.textContent=x;document.body.appendChild(e);setTimeout(()=>e.remove(),1600)};UI.overlay=h=>{ov.innerHTML=`<div class="sheet"><button class="close" onclick="UI.close()">×</button>${h}</div>`;ov.classList.remove('hidden')};UI.close=()=>ov.classList.add('hidden');UI.navMap=()=>S.modern?UI.confirmReturn():UI.map();UI.map=()=>{let x=Object.entries(D.locations).map(([id,l])=>`<button class="btn" onclick="C.go('${id}')"><b>${l[0]}</b><br><span class="muted">${id===S.place?'你在这里':'步行前往'}</span></button>`).join('');UI.render(`<div class="hero"><h2>青石镇 · ${C.clock()}</h2><p>Day ${S.day}</p></div><div class="card"><b>体力 ${S.energy}%</b><div class="bar energy"><i style="width:${S.energy}%"></i></div></div><div class="grid">${x}</div><button class="btn primary" onclick="UI.restChoice(false)">回土地庙休息</button>`)};UI.energyWarning=to=>UI.overlay(`<h3>体力不足</h3><p>可以先吃点东西，或者回土地庙休息。</p><button class="btn" onclick="UI.tryEat('${to}')">吃东西</button><button class="btn" onclick="UI.close();UI.restChoice(false)">回土地庙休息</button><button class="btn" onclick="UI.close()">取消</button>`);UI.tryEat=to=>{if(C.eat()){UI.close();C.go(to)}};UI.collapseEvent=ev=>UI.overlay(`<h3>你实在走不动了</h3><p><b>${ev[0]}</b></p><p>${ev[1]}</p><p>回去方式：${ev[2]}<br>花费：${ev[3]}文</p><button class="btn primary" onclick="UI.close();UI.restChoice(true)">到了土地庙……</button>`);UI.restChoice=()=>{S.place=S.home;UI.overlay(`<h3>土地庙 · 休息</h3><button class="btn" onclick="UI.close();C.rest(2)">休息2小时</button><button class="btn" onclick="UI.close();C.rest(4)">休息4小时</button><button class="btn primary" onclick="UI.close();C.sleep()">睡到第二天</button>`)};UI.bags=()=>UI.overlay(`<h3>背囊</h3>${UI.inv('穿越背囊',S.bags.travel)}${UI.inv('古代背囊',S.bags.ancient)}${UI.inv('土地庙储物',S.bags.home)}`);UI.inv=(n,i)=>`<div class="card"><b>${n}</b>${Object.entries(i.items).map(([k,v])=>`<div class="slot">${D.items[k].name} ×${v}${D.items[k].food&&i===S.bags.ancient?` <button onclick="C.eat();UI.bags()">吃</button>`:''}</div>`).join('')||'<p class="muted">空</p>'}</div>`;UI.rumors=()=>UI.overlay(`<h3>见闻</h3>${S.rumors.map(x=>`<div class="card">${x}</div>`).join('')||'<p class="muted">还没有见闻。</p>'}`);UI.relations=()=>UI.overlay(`<h3>人物</h3>${Object.entries(D.npcs).map(([k,n])=>`<div class="card"><b>${n[0]}</b> ♥${S.aff[k]??n[2]}<br><span class="muted">${n[1]}</span></div>`).join('')}`);UI.menu=()=>UI.overlay(`<h3>菜单</h3><button class="btn primary" onclick="C.save()">保存游戏</button><button class="btn" onclick="UI.loadGame()">读取存档</button><button class="btn danger" onclick="UI.restartConfirm()">重新开始</button>`);UI.loadGame=()=>{if(!C.load())return UI.toast('还没有存档');UI.close();UI.map();UI.toast('已读取存档')};UI.restartConfirm=()=>UI.overlay(`<h3>重新开始？</h3><p>当前未保存进度会丢失。</p><button class="btn danger" onclick="UI.close();C.restart()">确认重新开始</button><button class="btn" onclick="UI.menu()">取消</button>`);