window.Game=window.Game||{};Game.UI=Game.UI||{};(function(UI){var oldModern=UI.modern;UI.modern=function(){oldModern();var cards=UI.screen.querySelectorAll('.card'),s=Game.state,I=Game.Systems.Inventory,it=Game.Data.items.freshBag;if(!it)return;for(var i=0;i<cards.length;i++){var h=cards[i].querySelector('h3');if(h&&h.textContent==='货架'){var b=document.createElement('button');b.className='btn';b.id='buyFreshBag';b.textContent=it.name+' · ¥'+it.buy;cards[i].appendChild(b);b.onclick=function(){UI.sheet('<h3>购买 '+it.name+'</h3><p>一份可以处理最多5份当天鲜菜，或最多5条鲜鱼，让它们接下来两天仍按新鲜货计算。</p><p>已经变质的东西不能靠它重新变新鲜。</p><p>单价 ¥'+it.buy+'</p><select id="freshBagQty"><option>1</option><option>2</option></select><button id="confirmFreshBagBuy" class="btn primary">确认购买</button>');document.getElementById('confirmFreshBagBuy').onclick=function(){var q=+document.getElementById('freshBagQty').value,c=q*it.buy;if(s.rmb<c)return UI.toast('钱不够');if(!I.canAdd(s.bags.travel,'freshBag',q))return UI.toast('随身的小包放不下这么多');I.add(s.bags.travel,'freshBag',q);s.rmb-=c;s.refundable.freshBag=(s.refundable.freshBag||0)+q;UI.closeSheet();UI.modern();};};break;}}};var oldBags=UI.bags;UI.bags=function(){oldBags();if(Game.state.modern)return;var P=Game.Systems.Produce,F=Game.Systems.FishFreshnessMarket,produce=(Game.Data.produceIds||[]).filter(function(id){return P&&P.preservableQty(id)>0;}),fish=['fish','lakeFish','grassCarp','silverCarp','perch','mandarinFish'].filter(function(id){return Game.Data.items[id]&&F&&F.preservableQty(id)>0;});if(!produce.length&&!fish.length)return;var sheet=document.getElementById('sheet'),close=document.getElementById('closeSheet'),card=document.createElement('div');card.className='card';card.innerHTML='<h3>现代保鲜剂</h3><p class="muted">鱼和菜都能用。每份保鲜剂最多处理5份同类新鲜食材；已经过夜变质的不能恢复。</p>';produce.forEach(function(id){var btn=document.createElement('button');btn.className='btn preserveProduce';btn.dataset.id=id;btn.textContent='保鲜 '+Game.Data.items[id].name+'（最多5份）';card.appendChild(btn);});fish.forEach(function(id){var btn=document.createElement('button');btn.className='btn preserveFish';btn.dataset.id=id;btn.textContent='保鲜 '+Game.Data.items[id].name+'（最多5条）';card.appendChild(btn);});if(close)sheet.insertBefore(card,close);else sheet.appendChild(card);card.querySelectorAll('.preserveProduce').forEach(function(b){b.onclick=function(){var r=P.preserve(this.dataset.id);UI.toast(r.msg);UI.top();UI.bags();};});card.querySelectorAll('.preserveFish').forEach(function(b){b.onclick=function(){var r=F.preserve(this.dataset.id);UI.toast(r.msg);UI.top();UI.bags();};});};})(Game.UI);